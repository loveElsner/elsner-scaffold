/**
 * Responsive embeds
 */

.embed-responsive {
	position: relative;
	width: 100%;
	aspect-ratio: 16 / 9;
	overflow: hidden;
	border-radius: var(--radius-lg);

	& iframe,
	& video,
	& embed,
	& object {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		border: 0;
	}
}
